<div class="container-fluid">
  <div class="row">
    <div class="mapa">
      <% @cords.each do |cord| %>
        <a href="<%= exhibit_path(cord.exhibit_id) %>"><div id="<%= cord.cord %>"></div></a>
      <% end %>
     </map>
    </div>
  </div>
</div>  